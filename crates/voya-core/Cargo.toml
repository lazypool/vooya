[package]
name = "voya-core"
version = "0.0.0"
description = "Core runtime for Voya components."
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"
