---
"@vooya/compiler": patch
---

Strengthen the alpha release contract: packed packages now carry the declared
dual-license texts, coordinated package and artifact versions are verified, and
the compiler is authored in strict TypeScript while preserving its JavaScript
runtime entrypoints and generated component behavior. Add verified Vue and
React source-component quickstarts and a non-publishing CI release gate.
