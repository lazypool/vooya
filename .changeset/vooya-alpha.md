---
"@vooya/core": patch
"@vooya/vite-plugin": patch
"@vooya/vue": patch
"@vooya/react": patch
---

Ship portable application builds, typed component contexts, the Rust View API,
recoverable development rebuilds, custom Rust dependencies, and the `.voo`
formatter under the `@vooya` scope.
